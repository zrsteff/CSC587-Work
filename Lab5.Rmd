---
title: "Lab5"
author: "Zach Steffens"
date: "2025-10-01"
output: html_document
---
```{r}

# Fit a linear regression model
lm_model <- lm(Sepal.Length ~ Sepal.Width, data = iris)

# Model summary
summary(lm_model)

# Predictions for new horsepower values
new_data <- data.frame(Sepal.Width = c(1, 3, 5))
predictions <- predict(lm_model, new_data, interval = "confidence")
predictions

# Plot data with regression line
plot(iris$Sepal.Width, iris$Sepal.Length, 
     pch = 19, col = "blue", 
     xlab = "Sepal.Width", ylab = "Sepal.Length",
     main = "Linear Regression: Sepal.Length ~ Sepal.Width")
abline(lm_model, col = "red", lwd = 2)



```
